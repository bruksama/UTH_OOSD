# ===========================================
# SPTS - Application Configuration (DEV PROFILE)
# ===========================================

# Disable Firebase Authentication for local development
# This allows developers to test APIs without needing a valid Firebase service account JSON.
firebase.enabled=false
